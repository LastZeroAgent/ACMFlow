# 三数排序问题
 ## 题目描述
给定三个正整数 a, b, c，请将它们按从小到大的顺序排列并输出。你需要通过合理的比较和交换操作实现这一目标。

## 输入格式
输入仅一行，包含三个用空格分隔的正整数 a, b, c (1 ≤ a, b, c ≤ 10^9)。

## 输出格式
输出一行，包含三个按升序排列的正整数，用空格分隔。

## 数据范围
1. 本题可通过多次两两比较实现排序，也可借助临时变量辅助交换。
2. 注意处理存在重复数字的情况。
3. 时间复杂度应控制在 O(1)，因输入规模固定为3个数。

---
